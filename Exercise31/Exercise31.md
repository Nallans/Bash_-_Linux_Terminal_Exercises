# Exercise 31 — Variables + commands

Create variables for:

your project directory
a file extension
a search term

Then use those variables with commands you've already learned.

For example, instead of hardcoding:

find project -name "*.c"

you should be able to construct something where the directory and pattern come from variables.

The goal is to make your commands reusable.