# Set 3 Challenge — the pipeline puzzle 🧩

Finally, I'll give you a desired result without telling you which commands to use.

You have:

words.txt

containing many words, with duplicates and mixed ordering.

Produce an output that:

contains only lines containing a,
ignores case,
sorts them alphabetically,
removes duplicates,
displays only the first 10 results.

You should be able to build this from the tools you've learned rather than searching for a single magic command.